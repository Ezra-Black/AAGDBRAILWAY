# Use Nixpacks / Railway Node defaults.
# On Railway: keep the web service on `npm start`, and add a second
# service with start command `npm run worker:start` (same repo + env).
web: npm start
worker: npm run worker:start
